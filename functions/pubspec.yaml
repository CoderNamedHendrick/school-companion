name: school_companion_functions
description: Firebase Cloud Functions for School Companion Google Workspace integration.
publish_to: none

environment:
  sdk: ^3.12.1

dependencies:
  crypto: ^3.0.7
  firebase_admin_sdk: ^0.5.6
  firebase_functions: ^0.6.0
  google_cloud_firestore: ^0.5.5
  http: ^1.6.0
  shelf: ^1.4.2

dev_dependencies:
  build_runner: ^2.15.1
  lints: ^6.0.0
  test: ^1.31.0
